# ─── Database ──────────────────────────────────────────────────
# Each environment uses its own DATABASE_URL.
#
# How it works (@repo/env loads env files based on NODE_ENV):
#   1. Platform env vars (Render, Vercel) — highest priority
#   2. .env.{NODE_ENV}  (e.g. .env.development) — env-specific overrides
#   3. .env             — shared defaults / fallback
#
# Local dev:  copy this file to `.env` and set DATABASE_URL to your dev DB.
# Production: set DATABASE_URL on your hosting platform (Render dashboard).
#
# Example — two separate databases:
#   .env.development  →  DATABASE_URL=postgresql://user:pass@localhost:5432/cygobet_dev
#   .env.production   →  (not needed locally — set on Render)
#   .env              →  (shared non-secret defaults, or dev DATABASE_URL)
DATABASE_URL=postgresql://user:password@localhost:5432/cygobet_dev

# ─── Sports Data Provider ─────────────────────────────────────
# sportmonks | api-football | flashscore
SPORTS_DATA_PROVIDER=sportmonks

# SportMonks Config
SPORTMONKS_API_TOKEN=your_token
SPORTMONKS_FOOTBALL_BASE_URL=https://api.sportmonks.com/v3/football
SPORTMONKS_CORE_BASE_URL=https://api.sportmonks.com/v3/core

# Future: API-Football Config
# API_FOOTBALL_KEY=your_key
# API_FOOTBALL_BASE_URL=https://api-football-v1.p.rapidapi.com

# ─── Redis (optional) ─────────────────────────────────────────
# Enables multi-instance Socket.IO, caching, and distributed rate limiting
# REDIS_URL=redis://localhost:6379
